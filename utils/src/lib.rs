extern crate rust_decimal;

pub mod time;
pub mod math;
pub mod algorithm;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
